import React from 'react';

function TaskForm({ initialValues }) {
  const handleSubmit = (event) => {
    // Handler for handleSubmit
  };
  return (
    <div className="task-form-container">
      <h3>TaskForm</h3>
    </div>
  );
}

export default TaskForm;
