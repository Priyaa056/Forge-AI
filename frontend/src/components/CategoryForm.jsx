import React from 'react';

function CategoryForm({ initialValues }) {
  const handleSubmit = (event) => {
    // Handler for handleSubmit
  };
  return (
    <div className="category-form-container">
      <h3>CategoryForm</h3>
    </div>
  );
}

export default CategoryForm;
